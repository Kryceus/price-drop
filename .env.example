DB_HOST=localhost
DB_PORT=5432
DB_NAME=pricecompare
DB_USER=postgres
DB_PASSWORD=your_database_password_here
# Production hosts like Railway usually provide this.
# If DATABASE_URL is set, it is used instead of DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD.
DATABASE_URL=
APP_HOST=127.0.0.1
APP_PORT=8080
# Railway sets PORT automatically. If APP_PORT is blank, the app uses PORT, then 8080.
VITE_API_BASE=http://127.0.0.1:8080
FRONTEND_ORIGINS=http://127.0.0.1:3000,http://localhost:3000,capacitor://localhost
SESSION_COOKIE_SAMESITE=Lax
SESSION_COOKIE_SECURE=false
# For a deployed HTTPS backend used by the Android app, use:
# SESSION_COOKIE_SAMESITE=None
# SESSION_COOKIE_SECURE=true
FIREBASE_CREDENTIALS_PATH=./firebase-service-account.json
# Or set FIREBASE_CREDENTIALS_JSON to the full Firebase service-account JSON in production.
# FIREBASE_CREDENTIALS_JSON={"type":"service_account",...}
